#include <stdio.h>

int main() {

    int a, b, c;
    int *p1, *p2;

    printf("Enter the value of a and b: ");
    scanf("%d %d", &a, &b);
    p1 = &a;
    p2 = &b;

   
    c = *p1;
    *p1 = *p2;
    *p2 = c;

    printf("\nAfter swapping a = %d and b = %d", a, b,c);

    return 0;

}
